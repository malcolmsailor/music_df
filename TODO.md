# TODO

## Secondary mode follow-up

- [ ] Update `harmony/chords.py` so `get_rn_pc_cache` can resolve secondary mode (e.g., distinguish `V/Vm` from `V/V`)
- [ ] Update `add_chord_pcs` in `chord_df.py` to use `secondary_mode` column for mode-aware PC lookup (depends on the above)
